<controls:MetroWindow
    x:Class="Autoservice.Dialogs.AddOrderDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:command="http://www.galasoft.ch/mvvmlight"
    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:controls1="clr-namespace:ConstaSoft.Core.Controls;assembly=ConstaSoft.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:managers1="clr-namespace:ConstaSoft.Core.Controls.Managers;assembly=ConstaSoft.Core"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    Title="{Binding Title}"
    Width="900"
    Height="650"
    GlowBrush="{DynamicResource AccentColorBrush}"
    ResizeMode="NoResize"
    ShowMaxRestoreButton="False"
    ShowMinButton="False"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <DataTemplate DataType="{x:Type managers1:PanelManager}">
            <controls1:Panel />
        </DataTemplate>        
        <BooleanToVisibilityConverter x:Key="btv" />
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <command:EventToCommand Command="{Binding Loaded}" />
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <command:EventToCommand Command="{Binding Close}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="75" />
            </Grid.RowDefinitions>

            <GroupBox Header="Данные заказа" Margin="13,8,-3,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="5*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="5,0">
                        <StackPanel Margin="0,5">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="Bold"
                                Text="Номер" />
                            <TextBox
                                Margin="0,5,0,0"
                                IsEnabled="{Binding IsNew}"
                                Text="{Binding Order.PersonalNumber, Mode=OneWay}" />
                        </StackPanel>
                        <StackPanel Margin="0,5">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="Bold"
                                Text="Клиент" />
                            <ComboBox
                                Margin="0,5,0,0"
                                IsEnabled="{Binding IsNew}"
                                ItemsSource="{Binding Clients}"
                                SelectedItem="{Binding SelectedClient}" />
                        </StackPanel>
                        <StackPanel Margin="0,5">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="Bold"
                                Text="Транспортное средство" />
                            <ComboBox
                                Margin="0,5,0,0"
                                IsEnabled="{Binding IsNew}"
                                ItemsSource="{Binding Cars}"
                                SelectedItem="{Binding SelectedCar}" />
                        </StackPanel>
                        <StackPanel Margin="0,5">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="Bold"
                                Text="Зона ремонта" />
                            <TextBox
                                Margin="0,5,0,0"
                                IsReadOnly="False"
                                Text="{Binding Order.RepairZone}" />
                        </StackPanel>
                        <StackPanel Margin="0,5">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                FontWeight="Bold"
                                Text="Статус" />
                        <TextBlock 
                            Margin="0,5,0,0"
                            FontSize="12"
                            FontWeight="Regular"
                            Text ="{Binding Order.StringStatus}" />
                            <StackPanel Margin="0,7">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Text="Конечная цена заказа" />
                                <TextBox
                                    Margin="0,5,0,0"
                                    IsReadOnly="True"
                                    Text="{Binding TotalPrice, Mode=OneWay}" />
                            </StackPanel>
                            <StackPanel Margin="0,5">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Text="Способ оплаты" />
                                <ComboBox
                                    Margin="0,5,0,0"
                                    ItemsSource="{Binding Methods}"
                                    SelectedIndex="{Binding SelectedMethod}" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <Grid Grid.Column="1" Margin="5,0">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <GroupBox Margin="0,7" Header="Работы">
                            <GroupBox.HeaderTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding}"></TextBlock>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button VerticalAlignment="Center" ToolTip="Add Work" Command="{Binding RelativeSource={RelativeSource AncestorType=controls:MetroWindow}, Path=DataContext.AddWorkCommand}" Margin="5,0,0,0">
                                                <Rectangle Width="15" Height="15" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                                                    <Rectangle.OpacityMask>
                                                        <VisualBrush Visual="{StaticResource appbar_add}" />
                                                    </Rectangle.OpacityMask>
                                                </Rectangle>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </GroupBox.HeaderTemplate>
                            <ScrollViewer>
                                <DataGrid
                                        MinHeight="70"
                                        AutoGenerateColumns="False"
                                        CanUserAddRows="False"
                                        IsSynchronizedWithCurrentItem="True"
                                        ItemsSource="{Binding OrderWorks}">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="Наменование" Width="2*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox Margin="0, 5, 0, 0"
                                                                    Style="{DynamicResource VirtualisedMetroComboBox}"
                                                                    SelectedItem="{Binding Work, NotifyOnSourceUpdated=True, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                                                    controls:TextBoxHelper.Watermark="Выберите работу..."
                                                                    IsReadOnly="True"
                                                                    IsEditable="True"
                                                                    ItemsSource="{Binding DataContext.Works, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    MaxDropDownHeight="125"
                                                                    Text="{Binding Work.Name}" 
                                                                    DisplayMemberPath="Name"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Stretch"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn
                                                Binding="{Binding Price, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Header="Цена" />
                                        <DataGridTemplateColumn Header="Мастер" Width="2*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox Margin="0, 5, 0, 0"
                                                                    Style="{DynamicResource VirtualisedMetroComboBox}"
                                                                    SelectedItem="{Binding Master, NotifyOnSourceUpdated=True, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                                                    controls:TextBoxHelper.Watermark="Выберите мастера..."
                                                                    IsReadOnly="True"
                                                                    IsEditable="True"
                                                                    ItemsSource="{Binding DataContext.Masters, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    MaxDropDownHeight="125"
                                                                    DisplayMemberPath="Name"
                                                                    Text="{Binding Master.Name}"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Stretch"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Процент">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="50" VerticalAlignment="Bottom" MinWidth="50"></TextBox>
                                                    <TextBlock Text="%" FontFamily="Bold" VerticalAlignment="Bottom" FontSize="20"></TextBlock>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                        </GroupBox>
                        <GroupBox Margin="0,7" Header="Запчасти" Grid.Row ="1">
                            <GroupBox.HeaderTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding}"></TextBlock>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button VerticalAlignment="Center" ToolTip="Add SpareParts" Command="{Binding RelativeSource={RelativeSource AncestorType=controls:MetroWindow}, Path=DataContext.AddSparePartCommand}" Margin="5,0,0,0">
                                                <Rectangle Width="15" Height="15" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                                                    <Rectangle.OpacityMask>
                                                        <VisualBrush Visual="{StaticResource appbar_add}" />
                                                    </Rectangle.OpacityMask>
                                                </Rectangle>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </GroupBox.HeaderTemplate>
                            <ScrollViewer>
                                <DataGrid
                                        MinHeight="70"
                                        AutoGenerateColumns="False"
                                        CanUserAddRows="False"
                                        IsSynchronizedWithCurrentItem="True"
                                        ItemsSource="{Binding OrderSpareParts}">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="Наменование" Width="2*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox Margin="0, 5, 0, 0"
                                                                    Style="{DynamicResource VirtualisedMetroComboBox}"
                                                                    SelectedItem="{Binding SparePart, NotifyOnSourceUpdated=True, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                                                    controls:TextBoxHelper.Watermark="Выберите запчасть..."
                                                                    IsReadOnly="True"
                                                                    IsEditable="True"
                                                                    ItemsSource="{Binding DataContext.SpareParts, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    Text="{Binding SparePart.Name}"                                                               
                                                                    MaxDropDownHeight="125"                                                                 
                                                                    DisplayMemberPath="Name"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Stretch"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn
                                                Binding="{Binding Number, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Header="Количество" />
                                        <DataGridTemplateColumn Header="Источник" Width="2*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox Margin="0, 5, 0, 0"
                                                                    Style="{DynamicResource VirtualisedMetroComboBox}"
                                                                    SelectedIndex="{Binding Source,  Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                    controls:TextBoxHelper.Watermark="Выберите источник запчастей..."
                                                                    IsReadOnly="True"
                                                                    IsEditable="True"
                                                                    ItemsSource="{Binding DataContext.Sources, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    MaxDropDownHeight="125"
                                                                    Text="{Binding StringSource}"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Stretch"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                        </GroupBox>
                    </Grid>
                </Grid>                
            </GroupBox>
        <GroupBox Grid.Row="1" Margin="5,10" Header="Примечания к заказу">
            <ScrollViewer>
                <TextBox Text="{Binding Order.Notes}"></TextBox>
            </ScrollViewer>
        </GroupBox>
        <controls:Flyout
                Grid.Row="2"
                Height="75"
                CloseButtonVisibility="Collapsed"
                IsOpen="True"
                IsPinned="True"
                Position="Bottom"
                TitleVisibility="Collapsed">
                <Grid>
                    <ContentControl Content="{Binding Panel}" />
                </Grid>
            </controls:Flyout>
        </Grid>
</controls:MetroWindow>